<div class="container">
  <div class="row views">
  
    <div class="col-2 sidebar">
      <!-- サイドバー --> 
      <%= render 'public/posts/sidebar' %>
    </div>

    <div class="col-10 follower-index"> 
      <h2 class = "title"> 
        <%= link_to user_path(@user), class: 'title' do %>
          <%= @users.name %>
        <% end %>
        さんをフォローしているユーザー
      </h2> 
      <h5 class = "title"> <%= @users.count %>人にフォローされています</h5>
      <%= render 'follow_user',users:@users %>
      <br>
    </div>
  </div>
</div>